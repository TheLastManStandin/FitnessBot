[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --verbose
    --tb=short
    --strict-markers
    --strict-config
    --disable-warnings
markers =
    unit: unit tests
    integration: integration tests
    slow: slow tests
asyncio_mode = auto